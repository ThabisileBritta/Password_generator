# Password_generator
Password Generator — algorithm thinking (entropy calculation, character set logic), real-time UI updates, and the Web Crypto API for clipboard.writeText. The entropy meter shows CS depth.
